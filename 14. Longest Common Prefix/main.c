#include "../../school_21/libft/srcs/ft_putstr.c"

char *longestCommonPrefix(char **strs, int strsSize);

int	main(int argc, char *argv[])
{
	// char	input1[100][100] = {"Frog", " jumps", " !"};
	char	**input;
	int		maxlen;
	int		i;

	if (argc == 1)
		ft_putstr("longestCommonPrefix(argv, argc) is a.out");
	else
	{
		maxlen = 0;
		i = 0;
		while (i < (argc - 1))
		{
			
		}
		input = (char **)malloc((argc - 1) * sizeof(char *) + ((argc - 1) * (sizeof(char) * )
		int **array = malloc (nrows * sizeof(int *) + (nrows * (ncolumns * sizeof(int)));
		ft_putstr(longestCommonPrefix(argv, argc));
	}
	free(input)
	return (0);
}